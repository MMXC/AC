# 全局规则：优先使用 Context7 Skills（但避免无效搜索）

## 核心原则

**当任务可能受益于通用能力/最佳实践/成熟工具链时，优先使用 Context7 (ctx7) 搜索并安装合适 skill；当任务明显是仓库内业务修改时，直接实现。**

> 目标：减少“每次对话都搜索”带来的时间/噪音/失败（尤其在无网络或受限环境下），同时保留“能用现成 skill 就别重复造轮子”的收益。

## 工作流程

### 1. 任务开始时的决策流程（先判断是否值得搜）

当用户提出需求或任务时，按以下顺序执行：

~~~
用户需求
    ↓
0. 判断是否值得搜索（见下方“触发条件”）
    ├─ NO → 直接手动实现（必要时仅查本仓库）
    └─ YES → 继续下一步
    ↓
1. 使用 ctx7 搜索相关 skills（快进快出）
    ├─ 找到合适的 skill？
    │   ├─ YES → 安装并使用该 skill
    │   └─ NO → 继续下一步
    ↓
2. 使用 search-skill 搜索其他市场
    ├─ 找到合适的 skill？
    │   ├─ YES → 安装并使用该 skill
    │   └─ NO → 继续下一步
    ↓
3. 手动实现或创建自定义 skill
~~~

### 2. ctx7 的触发条件（何时“必须/建议/跳过”）

**必须/强烈建议使用 ctx7：**
- 需要引入或对齐某个成熟生态/工具链（例如：鉴权方案、数据库迁移、队列、缓存、OpenAPI、日志/观测、CI、部署、测试框架）
- 需要规范化/标准化产物（例如：ADR、RFC、运行手册、发布流程、代码生成）
- 需要跨项目通用实现且容易踩坑（例如：安全、性能、并发、序列化、时间/时区、加密、权限模型）
- 用户明确表示“找现成方案/最佳实践/skill”

**通常应跳过 ctx7：**
- 明确是仓库内的小改动/业务字段调整/接口参数校验/修 bug（已知文件路径或可在仓库内快速定位）
- 用户要求“不要安装新依赖/不要用现成 skill/直接改代码”
- 当前执行环境无网络/受限（无法安装/搜索）且不影响直接实现

### 3. 使用 ctx7 的步骤（省时版）

**步骤 1：搜索技能**

~~~bash
# 提取用户需求的关键词
# 使用 ctx7 搜索
npx ctx7 skills search {keywords}
~~~

**步骤 2：评估结果**
- 检查技能描述是否匹配需求
- 验证技能质量（来源、维护状态等）
- 确认技能兼容性（Cursor/Claude 等）
- 优先选择“少依赖、可直接落地、与当前技术栈匹配”的 skill（避免为一个小需求引入庞大框架）

**步骤 3：安装技能**

~~~bash
# 安装到当前项目
npx ctx7 skills install {project} {skill-name} --cursor

# 或安装到全局
npx ctx7 skills install {project} {skill-name} --global
~~~

**步骤 4：使用技能**
- 阅读已安装技能的 SKILL.md
- 按照技能指南执行任务
- 告知用户已使用的技能
- 若安装/拉取需要网络或权限：先说明原因并请求授权；若不可用则立刻降级到手动实现

### 4. “快速失败”约束（避免过度搜索）

- 搜索最多 1 轮 ctx7 + 1 轮 search-skill；仍无高匹配结果 → 立刻手动实现
- 如果需求已经能定位到具体文件/函数 → 不搜索，直接改
- 若任务可在 10 分钟内完成的简单改动 → 默认不搜索

### 5. 与其他技能的关系

**ctx7 vs search-skill：**
- **ctx7**：专门用于 Context7 Skills Registry（https://context7.com）
- **search-skill**：用于其他市场（GitHub、skills.sh 等）
- **优先级**：先使用 ctx7，再使用 search-skill

**ctx7 vs skill-manager：**
- **ctx7**：用于发现和安装新技能
- **skill-manager**：用于管理已安装技能的更新和生命周期

### 6. 实施要求

1. **按触发条件触发**：不是每次对话都搜；只有满足“必须/强烈建议”条件才搜
2. **透明沟通**：告知用户找到了什么技能，为什么选择它
3. **快速失败**：如果搜索无结果，快速转向手动实现，不要浪费时间
4. **记录使用**：在完成任务后，记录使用了哪些技能

## 相关资源

- Context7 Skills Registry: https://context7.com
- ctx7 CLI: https://github.com/upstash/context7
- ctx7 Skill: `.cursor/skills/ctx7/SKILL.md`
- search-skill: `.cursor/skills/search-skill/SKILL.md`

